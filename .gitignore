# Avhengigheter / bygg
node_modules/
.next/
dist/
build/
.turbo/
coverage/
*.tsbuildinfo
*.log

# Miljøvariabler — ALT unntatt eksempelet (hemmeligheter skal aldri i git)
.env
.env.*
!.env.example

# Vercel / OS
.vercel/
.DS_Store

# pnpm-probefiler + tmp-rester (Windows)
_tmp_*
*.tmptest

# Claude Code sine midlertidige worktrees. Disse er GITLINKS (mode 160000) —
# committes de, får neste maskin to ødelagte submodul-referanser uten
# .gitmodules. De hører til verktøyet, ikke til prosjektet.
.claude/worktrees/

# Visual Studio sin lokale arbeidsmappe (binære indeksfiler, ~4 MB).
# Maskinspesifikk — hører til IDE-en, ikke til prosjektet.
.vs/
