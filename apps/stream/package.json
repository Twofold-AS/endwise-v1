{
  "name": "@endwise/stream",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env --watch --experimental-strip-types src/dev.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@endwise/auth": "workspace:*",
    "@endwise/db": "workspace:*",
    "@endwise/modules": "workspace:*",
    "@hono/node-server": "^2.0.8",
    "hono": "^4.12.30"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
