# Rapport — 24.08.2026 — tenants.slett HTTP 412 etter db:setup (F5-26)

**Roadmap:** F5-26 (`done`) — nytt steg 0025
**Godkjenning:** produksjonsfeil på endwise.no (ikke ny flate)

---

## 1. Hva er gjort

| ID | Hva |
|---|---|
| **F5-26** | `POST /trpc/tenants.slett` ga HTTP 412 *etter* at Mikael kjørte `pnpm db:setup` på Scaleway (0023/0024). Ny request: 21:20:45 GMT+2, requestId `sdwsb-1787599245213-412242917e8b`, trace `ebf4fcc558a3a1a2dd3e58dcd874dabb`, cdg1, 235 ms, dpl_98PMuhbM77R4SZJiEPPryVBafJ4X (main, #30 9b08fe4 + #31 258437c). |

Rotårsak (verifisert i SQL/migrasjoner/kode, ikke gjettet mot prod-DB):

1. **0024 var `CREATE OR REPLACE` med samme signatur.** Drizzle-journalen hopper over 0024 når den allerede er merket kjørt. Body/policy kan ligge igjen fra før SELECT-policyene og Endwise-INSERT. `functions.sql` OR REPLACE alene gir ingen sjekk på at body faktisk ble byttet.
2. **Kodefeil i 0024 selv om body VAR ny:** INSERT/UPDATE WITH CHECK mot Endwise gikk via `select id from tenants where slug = 'endwise'` under tenants-RLS. `audit_log_slett_select` så bare slett-GUC — ny rad etter `tenant_id`-flytt matcher ikke SELECT.
3. **Leftover-løkka** registrerte bare FK ved DELETE, ikke stille 0-rader under RLS. `member` har ikke `tenant_id` og ble ikke telt.

Fikset i migrasjon **0025_slett_forhandler_endwise_guc**:
- `DROP FUNCTION slett_forhandler(uuid)` før `CREATE` (samme mønster som 0020/0021)
- `app.slett_endwise_id` — WITH CHECK/INSERT/SELECT uten tenants-subquery
- SELECT på `audit_log` / `erasure_requests` ser begge GUC-er (gammel og ny rad synlig)
- Kjent FK-rekkefølge (parts/stock/customers …) + dynamisk løkke + `EXISTS` på gjenværende rader
- `member` / `invitation` / `organization` sjekkes etter slett; stacked `constraint_name` hvis `DELETE FROM tenants` likevel treffer RESTRICT
- `db:grants` **exit 1** uten `slett_forhandler_rev=0025` i `prosrc`
- `audit_log` hard-slettes aldri; `user` slettes aldri; slug `endwise` nektes

Etter merge: **`pnpm db:setup`** på Scaleway. Loggen **MÅ** si `slett_forhandler rev=0025`.

---

## 2. Hva gikk galt

Forrige 0024 stolte på CREATE OR REPLACE + subquery mot `tenants` under FORCE RLS. Context7 MCP krevde auth og ble ikke brukt. Vercel runtime-logg for requestId timeoutet; fiksen er mot SQL/migrasjonskontrakten. Docker-eieren er superuser og bypasser FORCE RLS, så integrasjonstesten alene hadde ikke fanget prod-feilen; kildetestene er stand-in. Ingen lokal Postgres i denne økta — `forhandler-slett` og `force-rls` ble ikke kjørt mot DB.

---

## 3. Hvilke fikser ble gjort

1. 0025 DROP+CREATE av `slett_forhandler`.
2. `app.slett_endwise_id` i policyer (ikke `slug = 'endwise'`-subquery).
3. EXISTS-sjekk + stacked constraint ved RESTRICT.
4. `db:grants` verifiserer rev=0025; README dokumenterer det.

---

## 4. Neste fase / neste steg

Mikael kjører **`pnpm db:setup`** mot Scaleway etter merge. Loggen skal inneholde `slett_forhandler rev=0025`. Deretter slett den aktuelle forhandleren på `/endwise/forhandlere`. F1-07/F8-01 forblir `progress`.
