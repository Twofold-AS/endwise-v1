/**
 * @endwise/agents — plassholder.
 * Struktur reservert i F0 (techstack §4). Implementeres i F6-13.
 * Én mappe per agent: agent.ts + instructions.md + skills/.
 */
export const PLACEHOLDER = '@endwise/agents' as const;
