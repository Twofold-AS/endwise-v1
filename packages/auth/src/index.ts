/**
 * @endwise/auth — plassholder.
 * Struktur reservert i F0 (techstack §4). Implementeres i F1.
 * Better-Auth 1.x: organizations, passkey, phone-number-plugin, obligatorisk e-post-2FA, 60-min idle-timeout.
 */
export const PLACEHOLDER = '@endwise/auth' as const;
