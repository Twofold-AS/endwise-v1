import type { ExpressionId } from '../vendor/bloub/expressions.ts';

/**
 * Produktets hvileuttrykk: store, åpne øyne.
 * `neutre` er de små Grok-målte kapslene. `surpris` er store runde ovaler
 * fra eksisterende ExpressionId — ingen ny geometri.
 */
export const BLOUB_HVILE: ExpressionId = 'surpris';
